# Frontend — production build (vite build). Loaded automatically in production mode.
# Committed default; golive writes .env.production.local (gitignored) to override
# with the actual deployed domain if it differs.
VITE_API_BASE_URL=https://ticket-api.phiraphat.site/api
